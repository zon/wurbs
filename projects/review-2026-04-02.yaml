name: review-2026-04-02
description: Review deep module design of k8s component

requirements:
  - category: backend
    description: Eliminate duplication between config and k8s packages
    items:
      - Replace kubectlApply and BuildConfigmapYAML in core/config/configmap.go with calls to k8s.ApplyConfigmap and k8s.BuildConfigmapYAML
      - Remove duplicate kubectlApply implementation from config package
    passing: true

  - category: backend
    description: Ensure k8s module has a well-defined single area of responsibility
    items:
      - Review if any functions in k8s module belong to other domains (e.g., YAML building could be generic)
      - Consider extracting generic YAML building if needed elsewhere
    passing: false
